
#include "network.h"

void push_relabel(NetworkPointer network)
{
    /* VertexCollection source_neighbours = graph_neighbours_of(network->source); */
    /* size_t i; */
    /* for (i = 0; i < vertexcollection_length(source_neighbours); i++) { */
    /* } */
}
